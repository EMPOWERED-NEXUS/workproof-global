# Replace YOUR_COMPUTER_IP with your LAN IP (e.g. 192.168.1.20).
# Do not use localhost on a physical Android device.
# Restart Expo with a cleared cache after changing this value: npm run mobile:clear
EXPO_PUBLIC_API_URL=http://YOUR_COMPUTER_IP:4000/api/v1
